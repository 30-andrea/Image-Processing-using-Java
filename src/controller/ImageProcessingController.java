package controller;

/**
 * This interface defines a controller in an image processing application.
 */
public interface ImageProcessingController {

  /**
   * Executes image processing commands based on user input.
   */
  void execute();
}
